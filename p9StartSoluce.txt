Pour lancer le projet il a fallu 
/ gérer le gradle: 
	mettre à jour le plugin Gradle
	changer le targetSdkVersion to 31
	move to androidX (optionel, mais je détail)
	add classpath 'com.android.tools.build:gradle:7.0.4' in build.gradle (project)
	clear JCenter
	add mavenCentral()	

/ ajouter une ligne dans le manifest (android:exported="true")

/changer le nom du package pour le constraintLayout dans les layout (on est passé sur androidX)

/ dans la main activity, ligne 19, rempalcer second par main (this.textViewMain = findViewById(R.id.activity_main_activity_text_view_main);)
/ dans la main activity, ligne 36, convertir l'integer en string (this.textViewQuantity.setText(Integer.toString(quantity));)